// DEPENDENCIES
const express = require("express")
const app = express()
const PORT = 3000;

// index route
app.get("/",(req,res)=>{

})








// Listen Route
app.listen(PORT, ()=>{
  console.log("listening on PORT", PORT);
})
